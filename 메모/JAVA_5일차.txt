if 문

 - else가 한 문장일 경우 중괄호 생략 가능, 단 포함하는 것을 더 추천
 - 삼항연산자 : (변수명) ? (참일 경우 값) : (거짓일 경우 값)
 - 반복문 : 동일한 문장을 여러번 출력하여야 할 때 사용
    ▶어떠한 값이 되기까지 반복
    ▶횟수를 정해서 반복

    while (변수, 반복제한, 조건식 등) {
	(출력할 문장);
}
    ▶조건주기
     ① int i = 0;
   

while 문

for 문

  - for (①int i = 0; ②i < 5; ④i++) { 
	③System.out.println("반복");
     }
     ▶번호의 순서대로 작동
  - ① ② ④ 비어있어도 작동함, ①변수가 필요없을 때 ②비었을 경우 true ④조건이 필요 없을 때
  - ①에서 만든 변수는 for문 밖에서 사용불가
     
